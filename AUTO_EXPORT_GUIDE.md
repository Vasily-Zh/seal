# Руководство по автоматическому экспорту

## Обзор

Система автоматического экспорта позволяет в один клик создать полный пакет файлов из проекта конструктора печатей и штампов. Эта функция объединяет векторизацию, генерацию изображений и архивирование в единый процесс.

## 🚀 Ключевые возможности

### Векторизация текста
- Автоматическое преобразование текста в SVG paths
- Сохранение оригинальных шрифтов (локальные + Google Fonts)
- Поддержка текста по кругу и обычного текста
- Кеширование шрифтов для ускорения повторной обработки

### Форматы экспорта
- **PNG с фоном** - высокое качество 4000×4000px
- **PNG прозрачный** - без фона для наложения
- **PDF документ** - готов для печати
- **SVG векторизованный** - с текстом в виде paths
- **SVG оригинальный** - с текстовыми элементами

### Персонализация
- Настраиваемые имена файлов
- Автоматическое добавление даты
- Выбор качества векторизации
- Настройка максимального размера изображений

## 📱 Как использовать

### Через интерфейс

1. **Откройте ваш проект** в конструкторе печатей
2. **Нажмите кнопку "Автоэкспорт"** в заголовке приложения (иконка молнии ⚡)
3. **Настройте параметры** в открывшемся диалоге:
   - Выберите нужные форматы файлов
   - Включите/выключите векторизацию текста
   - Настройте качество и размеры
   - Укажите имя файла (опционально)
4. **Нажмите "Начать экспорт"**
5. **Следите за прогрессом** в реальном времени
6. **Получите ZIP архив** со всеми выбранными форматами

### Программное использование

```typescript
import { autoExportProject, AutoExportOptions } from '../utils/export';

// Пример использования
const options: AutoExportOptions = {
  vectorizeText: true,              // Векторизовать текст
  includePNG: true,                 // PNG с фоном
  includeTransparentPNG: true,      // PNG прозрачный
  includePDF: true,                 // PDF документ
  includeVectorizedSVG: true,       // Векторизованный SVG
  includeOriginalSVG: true,         // Оригинальный SVG
  filename: 'my_stamp',            // Имя файла (без расширения)
  maxSize: 4000,                   // Максимальный размер PNG
  quality: 'high'                  // Качество: high/medium/low
};

const svgElement = document.getElementById('stamp-canvas') as SVGSVGElement;
const elements = useStampStore(state => state.elements); // Элементы проекта
const projectName = 'Моя печать';

await autoExportProject(
  svgElement,
  elements,
  projectName,
  options,
  (progress, message) => {
    console.log(`Прогресс: ${progress}% - ${message}`);
  }
);
```

## ⚙️ Настройки

### Форматы файлов

| Формат | Описание | Использование |
|--------|----------|---------------|
| PNG с фоном | Белый фон, высокое разрешение | Печать, презентации |
| PNG прозрачный | Без фона, только контент | Наложение на другие изображения |
| PDF | Векторный формат для печати | Профессиональная полиграфия |
| SVG векторизованный | Все элементы как paths | CorelDRAW, Inkscape |
| SVG оригинальный | Текстовые элементы сохранены | Веб-использование, редактирование |

### Качество векторизации

- **Высокое** - максимальное качество, медленная обработка
- **Среднее** - баланс качества и скорости
- **Низкое** - быстрая обработка, базовое качество

### Размеры изображений

- **1000-2000px** - веб-использование, превью
- **4000px** - рекомендуется для печати высокого качества
- **6000-8000px** - профессиональная полиграфия

## 🔧 Технические детали

### Векторизация текста

Система автоматически:

1. **Извлекает шрифты** из элементов проекта
2. **Загружает шрифты** через кешированную систему
3. **Конвертирует глифы** в SVG paths
4. **Сохраняет трансформации** (позиции, размеры, цвета)
5. **Оптимизирует результат** для минимального размера

### Обработка больших изображений

- Автоматическое масштабирование до указанного максимума
- Сохранение пропорций
- Оптимизация памяти для PNG 4000×4000
- Пакетная обработка для ускорения

### Совместимость

#### CorelDRAW
- ✅ Все SVG файлы открываются корректно
- ✅ Текст сохраняет возможность редактирования
- ✅ Векторизованные элементы идеальны для печати
- ✅ Поддержка прозрачности

#### Inkscape
- ✅ Полная совместимость
- ✅ Оптимизированные SVG для минимального размера
- ✅ Поддержка всех элементов проекта
- ✅ Сохранение слоев и группировки

#### Adobe Illustrator
- ✅ Импорт всех форматов
- ✅ Сохранение редактируемости текста
- ✅ Поддержка цветовых пространств

## 🎯 Рекомендации по использованию

### Для печати
1. Используйте максимальный размер PNG (4000px+)
2. Включите векторизацию текста
3. Экспортируйте PDF для векторного качества
4. Добавьте SVG векторизованный для редактирования

### Для веб-использования
1. Уменьшите размер PNG до 2000px
2. Экспортируйте только PNG с фоном и оригинальный SVG
3. Отключите векторизацию для меньшего размера файла

### Для презентаций
1. PNG с фоном в высоком разрешении
2. SVG оригинальный для масштабирования
3. Среднее качество векторизации

## 🐛 Устранение неполадок

### Медленная векторизация
- Уменьшите качество до 'medium' или 'low'
- Уменьшите размер изображения
- Отключите векторизацию текста если не нужна

### Большие файлы
- Отключите неиспользуемые форматы
- Уменьшите максимальный размер PNG
- Используйте низкое качество векторизации

### Проблемы с шрифтами
- Шрифты загружаются автоматически
- Система использует fallback'и при отсутствии шрифтов
- Локальные шрифты имеют приоритет

### Ошибки экспорта
- Проверьте наличие элементов на холсте
- Убедитесь что холст имеет правильный ID 'stamp-canvas'
- Перезагрузите страницу при критических ошибках

## 📈 Производительность

### Ожидаемое время обработки

| Сложность проекта | Время экспорта | Рекомендации |
|-------------------|----------------|--------------|
| Простой (1-3 элемента) | 5-10 секунд | Стандартные настройки |
| Средний (4-10 элементов) | 15-30 секунд | Высокое качество |
| Сложный (10+ элементов) | 30-60 секунд | Низкое качество |

### Оптимизация скорости
- Используйте кеширование шрифтов
- Выключайте неиспользуемые форматы
- Уменьшайте размеры для предпросмотра

## 🔒 Безопасность

- Все операции выполняются на клиенте
- Никакие данные не отправляются на сервер
- Файлы генерируются локально в браузере
- ZIP архивы создаются без сжатия для скорости

## 📝 Обновления

### Версия 1.0 (текущая)
- ✅ Полный цикл автоматического экспорта
- ✅ Векторизация текста
- ✅ Поддержка всех основных форматов
- ✅ UI диалог с настройками
- ✅ Прогресс-бар и обратная связь

### Планируемые улучшения
- [ ] Экспорт в дополнительные форматы (AI, EPS)
- [ ] Пакетная обработка нескольких проектов
- [ ] Облачное сохранение результатов
- [ ] Шаблоны настроек экспорта
- [ ] Интеграция с принтерами

---

*Эта документация актуальна для версии 1.0 автоматического экспорта.*
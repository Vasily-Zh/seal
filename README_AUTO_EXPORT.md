# 🚀 Система автоматического экспорта с векторизацией

## ✅ Реализовано

Полностью автоматизированная система экспорта пользовательских проектов, которая генерирует все необходимые форматы в одном ZIP-архиве.

## 📦 Что включено в экспорт

При нажатии кнопки **"Автоэкспорт"** система создаёт ZIP-архив со следующими файлами:

1. **PNG с фоном** (4000×4000 пикселей)
   - Высокое разрешение для печати
   - Белый фон
   - Все элементы сохранены

2. **PNG с прозрачностью** (4000×4000 пикселей)
   - Прозрачный фон
   - Готов для наложения на другие изображения

3. **SVG векторизованный через Potrace**
   - Чистые векторные пути (path-элементы)
   - Гарантированно открывается в CorelDRAW
   - Сохранены цвета и форма
   - Текст по кругу преобразован в векторные контуры

4. **SVG оригинальный**
   - Редактируемый SVG с оригинальной структурой
   - Сохранены все элементы как есть

5. **PDF документ**
   - Высокое качество для печати
   - Сохранены пропорции

## 🎯 Как использовать

### Шаг 1: Создайте дизайн
- Используйте инструменты конструктора для создания печати/штампа
- Добавьте текст, круги, иконки, изображения

### Шаг 2: Запустите автоэкспорт
1. Нажмите кнопку **"Автоэкспорт"** в заголовке (фиолетовая кнопка с иконкой пакета)
2. В открывшемся диалоге нажмите **"Начать экспорт"**
3. Дождитесь завершения (отображается прогресс-бар)

### Шаг 3: Получите ZIP-архив
- Архив автоматически загрузится в папку Downloads
- Имя файла: `{имя_проекта}_export_{дата_время}.zip`

## 🛠️ Технические детали

### Векторизация через Potrace WASM

Система использует библиотеку Potrace (портированную в WebAssembly) для векторизации растровых изображений:

- **Алгоритм**: Potrace - профессиональный инструмент для трассировки bitmap → vector
- **Качество**: Оптимизированные параметры для четких контуров
- **Совместимость**: Результат работает в CorelDRAW, Inkscape, Adobe Illustrator
- **Цвета**: Сохраняются все цвета из оригинального изображения

### Архитектура

```
src/utils/
├── autoVectorize.ts       # Модуль векторизации через Potrace
└── autoExport.ts          # Главный модуль автоэкспорта

src/components/Header/
├── AutoExportDialog.tsx   # UI диалог с прогресс-баром
└── Header.tsx             # Кнопка запуска автоэкспорта
```

### Основные функции

```typescript
// Векторизация PNG → SVG
vectorizeColoredPNG(imageDataUrl: string, config?: PotraceConfig): Promise<string>

// Автоэкспорт всего проекта
autoExportToZIP(
  svgElement: SVGSVGElement,
  config?: AutoExportConfig
): Promise<AutoExportResult>

// Скачивание ZIP
downloadAutoExport(
  svgElement: SVGSVGElement,
  config?: AutoExportConfig
): Promise<void>
```

### Конфигурация

```typescript
interface AutoExportConfig {
  projectName?: string;        // Имя проекта для файлов
  pngSize?: number;            // Размер PNG (по умолчанию 4000)
  vectorizeConfig?: {
    threshold?: number;        // Порог ч/б (0-255)
    turnPolicy?: string;       // Стратегия поворота
    turdSize?: number;         // Подавление шума
    alphaMax?: number;         // Оптимизация кривых
    optCurve?: boolean;        // Оптимизация Безье
    optTolerance?: number;     // Толерантность оптимизации
  };
  onProgress?: (stage: string, progress: number) => void;
}
```

## 🎨 Работа с результатами

### CorelDRAW
1. Распакуйте ZIP
2. Откройте файл `*-vectorized.svg`
3. Все контуры редактируемы
4. Можно изменять цвета, масштабировать без потери качества

### Inkscape
1. Откройте `*-vectorized.svg` или `*-original.svg`
2. Векторизованная версия - чистые пути
3. Оригинальная версия - редактируемые элементы

### Печать
- Используйте PNG с фоном для растровой печати
- Используйте PDF для векторной печати
- Разрешение 4000×4000 достаточно для качественной печати

### Веб
- PNG с прозрачностью идеален для веб-сайтов
- Оригинальный SVG можно встраивать в HTML

## ⚡ Производительность

### Время обработки (примерное)
- **Простой дизайн** (1-3 круга, текст): 5-10 секунд
- **Средний дизайн** (несколько элементов): 15-25 секунд
- **Сложный дизайн** (много элементов, текст по кругу): 25-40 секунд

### Этапы обработки
1. Генерация PNG с фоном (10%)
2. Генерация PNG с прозрачностью (25%)
3. Векторизация PNG → SVG через Potrace (50%)
4. Генерация PDF (75%)
5. Добавление оригинального SVG (85%)
6. Создание ZIP-архива (95%)
7. Готово! (100%)

## 🔧 Решение проблем

### Векторизация работает некорректно
- Попробуйте изменить порог `threshold` в конфигурации
- Убедитесь, что изображение имеет достаточный контраст

### Экспорт занимает слишком много времени
- Это нормально для сложных дизайнов
- Векторизация - вычислительно затратный процесс
- При необходимости используйте обычный экспорт (кнопка "Скачать")

### ZIP-архив не скачивается
- Проверьте настройки браузера
- Убедитесь, что разрешены загрузки файлов
- Попробуйте другой браузер

### SVG не открывается в CorelDRAW
- Используйте файл `*-vectorized.svg` (не `-original.svg`)
- Векторизованная версия гарантированно совместима
- Убедитесь, что используете актуальную версию CorelDRAW

## 📝 Примечания

### Текст по кругу
- При векторизации текст преобразуется в контуры
- Исходный шрифт сохраняется визуально
- Редактирование текста после векторизации невозможно
- Для редактируемого текста используйте оригинальный SVG

### Шрифты
- Система использует шрифты, уже применённые при создании дизайна
- Векторизация сохраняет визуальное отображение текста
- Не требуется установка шрифтов на целевой системе

### Размер файлов
- PNG: ~500 KB - 3 MB (зависит от сложности)
- SVG векторизованный: ~50 KB - 500 KB
- SVG оригинальный: ~10 KB - 100 KB
- PDF: ~100 KB - 1 MB
- ZIP общий: ~600 KB - 4 MB

## 🎉 Преимущества системы

✅ **Один клик** - все форматы сразу
✅ **Потrace WASM** - профессиональная векторизация
✅ **CorelDRAW готов** - гарантированная совместимость
✅ **Высокое качество** - 4000×4000 пикселей
✅ **Прогресс в реальном времени** - видно, что происходит
✅ **Без сервера** - всё на клиенте, быстро и безопасно
✅ **Персонализированные имена** - удобно для архивации

---

## 📚 Для разработчиков

### Установка зависимостей
```bash
npm install esm-potrace-wasm jszip jspdf
```

### Подключение
```typescript
import { downloadAutoExport } from './utils/autoExport';
```

### Использование
```typescript
const svgElement = document.getElementById('stamp-canvas') as SVGSVGElement;

await downloadAutoExport(svgElement, {
  projectName: 'Моя печать',
  pngSize: 4000,
  onProgress: (stage, progress) => {
    console.log(`${stage}: ${progress}%`);
  }
});
```

---

*Система автоматического экспорта с векторизацией через Potrace WASM*
*Реализовано: 27 ноября 2025*

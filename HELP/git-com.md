
### Инициализация и настройка
```bash
git init                    # Создать новый репозиторий
git clone <url>             # Склонировать удаленный репозиторий
git config --global user.name "Ваше Имя"    # Настроить имя
git config --global user.email "ваш@email.com" # Настроить email
```

---

## ⚙️ Основной рабочий процесс
```bash
git status                  # Показать состояние файлов
git add <file>              # Добавить файл в индекс
git add .                   # Добавить все файлы
git commit -m "сообщение"   # Создать коммит
git push                    # Отправить изменения на сервер
git pull                    # Загрузить изменения с сервера
```
git checkout -b dev-5       создаю новую ветку
git push --set-upstream origin dev-5  пушу ее на сервер
---

## 📊 Просмотр информации
```bash
git log                     # История коммитов
git log --oneline           # Краткая история
git diff                    # Показать изменения
git show <commit>           # Показать конкретный коммит
```

---

## 🌿 Ветвление (Branching)
```bash
git branch                  # Показать ветки
git branch <name>           # Создать новую ветку
git checkout <branch>       # Переключиться на ветку
git checkout -b <branch>    # Создать и переключиться 
git push -u origin <branch> # первый пуш должен быть таким

git merge <branch>          # Слить ветки находясь в рабочей 
```

---

## ⚡ Работа с удаленным репозиторием
```bash
git remote -v               # Показать удаленные репозитории
git push origin <branch>    # Отправить ветку на сервер
git fetch                   # Загрузить изменения без слияния
```

---

## 🔄 Отмена изменений
```bash
git restore <file>          # Отменить изменения в файле
git reset <commit>          # Отменить коммиты
git checkout -- <file>      # Восстановить файл
```

---

## 🏷️ Работа с тегами
```bash
git tag                     # Показать теги
git tag v1.0.0              # Создать тег
git push --tags             # Отправить теги на сервер
```

---


## 🚪 Выход из git log
**Для выхода из режима просмотра `git log` используйте:**
- `q` — основной способ выхода  
- `Z` — альтернативный способ в некоторых системах

**Навигация в git log:**
- `Пробел` или `f` — пролистать вперед  
- `b` — пролистать назад  
- `Enter` — пролистать на одну строку  
- `/ + текст` — поиск  
- `q` — выход


git fetch  --  обновляет информацию о состоянии удалённого репозитория
git diff main origin/main  — покажет, чем твоя локальная ветка main отличается от версии на GitHub

Если ветка уже была удалена на GitHub, и ты хочешь очистить “ссылки” на неё:

git fetch -p



# Пришел домой и пытаюсь синхронизовать, а код измененный ===

error: Your local changes to the following files would be overwritten by checkout:
Git не позволяет переключаться на другую ветку, если у тебя есть несохранённые изменения

Отменить локальные изменения (если они не нужны)
git checkout -- .
git checkout (ветка-...)

# то же, но оно не показывает то что я локально менял, а я менял!
Если ты менял файлы локально и хочешь их откатить на версию с сервера, то:    

  git reset --hard origin/ветка

  Это откатит всё на текущей ветке (ветке) на версию с сервера и удалит все     
  твои локальные изменения.

  # Т.Е. если я работаю, и что-то на нравится жму команды
  git fetch origin
  git reset --hard origin/(название ветки)